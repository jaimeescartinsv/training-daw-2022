// Listar todos los divs
const allDivs = document.querySelectorAll('div');
console.log(allDivs);

// Listar todos los elementos con la clase 'buttons'
const buttonElements = document.querySelectorAll('.buttons');
console.log(buttonElements);

// Listar 1 div con clase 'buttons'
const oneButtonDiv = document.querySelector('.buttons');
console.log(oneButtonDiv);

// Listar TODOS los divs con clase 'buttons'
const allButtonDivs = document.querySelectorAll('.buttons');
console.log(allButtonDivs);